{
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "bazel build -c dbg src:cpplox --spawn_strategy=local",
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ],
    "version": "2.0.0"
}